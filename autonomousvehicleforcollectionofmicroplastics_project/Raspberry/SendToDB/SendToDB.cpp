
// Function for weight 
void SendWeight(void)
{
	int weight = GetWeight();
	
	//send it at base
	if (At_Base == true)
	{
		// Code for sending weight to dp
	}
	else 
		// sumtin? 
}

// Function for error cheacking
void SendError(void)
{
	// ErrorLogger[0] = its raining 
	// ErrorLogger[1] = its hot! 
	// ErrorLogger[2] = its humid! 
	
	if (ErrorCheck == true)
	{
		if (ErrorLogger[0] == 1)
			// its raining
		if (ErrorLogger[1] == 1)
			// its hot! 
		if (ErrorLogger[2] == 1)
			// its humid! 
		else	
			// Something else is wrong AHH! 
	}
}